//This is a special class that represents a group of constants (unchangeable variables).
//we uses these values to mark what state as object is.
public enum ObjectState {
    idle(),
    walking(),
    stunned(),
    attacking(),    
}


